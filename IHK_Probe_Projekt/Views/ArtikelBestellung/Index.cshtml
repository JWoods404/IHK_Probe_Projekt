@model IEnumerable<IHK_Probe_Projekt.Models.ArtikelBestellung>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArtikelId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BestellungId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.KundeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArtikelId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BestellungId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.KundeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
